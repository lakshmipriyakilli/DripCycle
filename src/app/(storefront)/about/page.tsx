import { ContentPageRenderer } from '@/components/storefront/ContentPageRenderer'
export const metadata = { title: 'About DripCycle', description: 'Learn about DripCycle — premium curated thrift fashion.' }
export default function AboutPage() { return <ContentPageRenderer slug="about" /> }
